/**
 * Core Module Barrel Export
 */

// storage utilities
export { getItem, isStorageAvailable, setItem } from "./storage.ts";

// theme configuration
export { type Theme, THEME_CONFIG } from "./config.ts";
